main(List<String> args) {
  //字符串定义方式
  var msg = 'hello';
  var msg1 = "ddd";

  //字符串的拼接(${变量})

  // 变量定义最好使用let标识符

  final name = 'why';
  final age =22;

  //如果{}是一个表达式，则不能省略，是一个变量能省略 
  print("name:$name age:$age");

}